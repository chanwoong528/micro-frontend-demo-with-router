import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  standalone: false,
  template: `
    <h1>{{ title }}</h1>
  `
})
export class AppComponent {
  title = 'angular-ap123123123p';
}
